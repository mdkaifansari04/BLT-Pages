{
  "name": "blt-pages",
  "version": "1.0.0",
  "description": "OWASP BLT Pages – static bug-reporting landing page",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
